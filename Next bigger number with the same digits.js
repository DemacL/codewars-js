function nextBigger(n) {
    let digArr = [...('' + n)];
    if (digArr.sort((x, y) => y - x).join('') === ('' + n)) {
        return -1;
    }else{
        for (let i = 0; i < digArr.length; i++) {
            const element = digArr[i];
            if()
            
        }
    }
}